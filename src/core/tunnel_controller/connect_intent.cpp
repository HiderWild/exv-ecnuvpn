#include "core/tunnel_controller/connect_intent.hpp"

namespace exv::core {
// Intent model types are defined in the header so tests and workflow owners can
// share the same lightweight value objects without introducing storage globals.
} // namespace exv::core

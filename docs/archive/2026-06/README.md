# 2026-06 文档归档

本目录存放 2026-06 之前已失效、已被后续实现取代、或仅保留历史参考价值的计划、检查清单、阶段报告与验证材料。

保留在 `docs/` 根目录下的文件应优先视为当前有效规范；若你在这里看到同主题的历史文档，请把它当作背景记录，而不是当前实现真相。

归档分类：

- `checklists/`：阶段性清单、发布门禁、回归矩阵
- `plans/`：历史迁移/重构/合并计划
- `reports/`：阶段总结、已知问题、收尾报告
- `validation/`：时间点相关的验证记录
- `merge-playbooks/`：历史合并执行手册
- `security/`：时间点相关的安全审查记录
- `architecture/`：针对特定阶段任务的架构验证笔记
- `superpowers/`：历史 superpowers 计划/spec 文档

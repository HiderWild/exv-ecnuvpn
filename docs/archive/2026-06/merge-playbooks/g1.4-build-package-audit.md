# G1.4 Packaging, Runtime, and Build Script Audit

> Date: 2026-05-21
> Branch: integration/platform-convergence-next (58074c5)
> Owner: team-lead (build/package lane)

## Findings

### 1. CMakeLists.txt — Platform source lists correct (PASS)

The CMakeLists.txt correctly includes:
- Platform-specific source lists for win32, darwin, linux
- Test targets: vpn_runtime_test, platform_status_models_test, tunnel_script_contract_test, app_api_runtime_policy_test, crypto_roundtrip_test
- Webui asset embedding via `scripts/embed_assets.py`
- All test targets build and pass on macOS

### 2. CMakePresets.json — Presets exist for both platforms (PASS)

Presets configured for `macos-release` and `windows-release` build configurations.

### 3. Build scripts — No stale rehearsal paths (PASS)

- `scripts/build-macos.sh`: builds native + webui for macOS
- `scripts/validate-merge-prep-macos.sh`: validates build + tests
- `scripts/build-windows.ps1`: builds native + webui for Windows
- `scripts/validate-merge-prep-windows.ps1`: validates build + tests on Windows

No references to stale rehearsal worktree paths found in build scripts.

### 4. webui/package.json — Desktop build scripts correct (PASS)

- `npm run build`: Vite web build
- `npm run build:electron`: Electron desktop build
- `npm run desktop:build`: Full desktop packaging
- `npm run prepare:native`: Stages native binaries via `prepare-native.cjs`

### 5. webui/scripts/prepare-native.cjs — Stages native binaries (PASS)

The script stages native binaries for both platforms:
- Windows: exv.exe, exv-helper.exe, MinGW runtime DLLs, OpenConnect runtime, wintun.dll
- macOS: staged/codesigned OpenConnect runtime and dylibs

### 6. Build outputs — Platform-separated (PASS)

Build outputs go to:
- macOS: `build/macos/cpp/` for native, `build/macos/electron/` for desktop
- Windows: `build/windows/cpp/` for native, `build/windows/electron/` for desktop

No cross-contamination between platform build outputs.

### 7. Sync-conflict artifacts — ISSUE (G2 repair candidate)

Sync-conflict files in build/packaging areas:
- `CMakeLists.sync-conflict-*.txt`
- `.sync-conflict-*.gitignore`
- `webui/package.sync-conflict-*.json`
- `webui/scripts/prepare-native.sync-conflict-*.cjs`
- Plus build directory artifacts in `build/macos/cpp/`

These should be deleted.

### 8. Stale rehearsal worktree references — ISSUE (G2 cleanup)

`.claude/worktrees/rehearsal-windows-new/` and `.claude/worktrees/rehearsal-windows-wrapper-6/` still exist with sync-conflict files. These should be cleaned up.

## Validation

- `cmake --build --preset macos-release`: PASS
- `ctest --preset macos-release --output-on-failure`: 5/5 PASS
- `cd webui && npm run build`: PASS (201ms)
- `npm run build:electron`: PASS

## Summary

| Check | Result |
|-------|--------|
| CMakeLists platform source lists | PASS |
| CMakePresets for both platforms | PASS |
| Build scripts no stale paths | PASS |
| Package build scripts correct | PASS |
| prepare-native.cjs stages correctly | PASS |
| Build outputs platform-separated | PASS |
| macOS build + tests | PASS |
| webui build + electron build | PASS |
| Sync-conflict artifacts | ISSUE — needs cleanup in G2 |
| Stale rehearsal worktrees | ISSUE — needs cleanup in G2 |

## G2 Repair Candidates

1. Delete all `*.sync-conflict-*` files in build/packaging areas (CMakeLists, .gitignore, package.json, prepare-native.cjs)
2. Delete stale rehearsal worktrees: `.claude/worktrees/rehearsal-windows-new/`, `.claude/worktrees/rehearsal-windows-wrapper-6/`
3. Clean build directory sync-conflict artifacts